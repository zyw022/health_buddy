{
  "name": "@health-buddy/decision-engine",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@health-buddy/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "^1.6.0"
  }
}
