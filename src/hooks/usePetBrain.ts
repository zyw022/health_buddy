// Action decisions are driven entirely by usePromptScheduler (scheduled bursts).
// This hook is intentionally a no-op; the pet defaults to idle via petStore initial state.
export function usePetBrain(): void {}
