{
  "name": "@health-buddy/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch dist/index.js",
    "dev:python": "python -m uvicorn src.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@health-buddy/shared-types": "*",
    "@health-buddy/decision-engine": "*",
    "@health-buddy/health-engine": "*",
    "hono": "^4.4.0",
    "better-sqlite3": "^9.6.0",
    "zod": "^3.23.8",
    "@hono/zod-validator": "^0.2.2"
  },
  "devDependencies": {
    "typescript": "*",
    "@types/better-sqlite3": "^7.6.11"
  }
}
