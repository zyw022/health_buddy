# TODO(phase-2): 实现本地多服务联调
#
# services:
#   server:
#     build: ../apps/server
#     ports: ["8000:8000"]
#     volumes: ["./data:/data"]
#     environment:
#       - DB_PATH=/data/health-buddy.db
#
#   mobile-mock:
#     build: ./mock-mobile
#     environment:
#       - SERVER_URL=http://server:8000
#       - USER_ID=local-test-user

version: "3.9"
services: {}
